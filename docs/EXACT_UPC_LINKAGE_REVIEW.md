# Exact UPC linkage review

This review set extracts every CPSC ↔ SaferProducts pair connected by an
exact UPC intersection.

These pairs are candidate high-confidence identity links, not automatically
declared ground truth. Their source identifiers, dates and textual evidence
are retained so they can be manually reviewed before being used as evaluation
labels.
