# Research

This directory contains reproducible experiments for SafeSKU.

Every experiment should record:

- code version
- dataset version
- time window
- category subset
- random seed
- model/configuration
- metrics
- output artifact

Do not evaluate on records that occur after the simulated decision time.
